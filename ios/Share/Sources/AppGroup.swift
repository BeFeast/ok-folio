import Foundation

/// App Group shared by the OKFolio app and the OKFolioShare extension.
/// Duplicate of `ios/App/Sources/AppGroup.swift` (extensions cannot import
/// app sources) — keep both in sync.
enum AppGroup {
    static let identifier = "group.com.befeast.okfolio"
}

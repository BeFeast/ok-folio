// App version shown in the Settings About panel. Injected at build time from
// dashboard/package.json via Vite's `define` (see vite.config.ts) so the
// displayed version tracks a single real source instead of a hardcoded literal.
export const APP_VERSION = __APP_VERSION__;
